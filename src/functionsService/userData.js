// const todosData = todos.map(({ userId, title, completed }) => {
//     return { userId, title, completed }
//   })
//   const usersData = users.map(({ id, name }) => {
//     const nameLabel = abbreviation(name)
//     return { id, nameLabel }
//   })
//   function newData() {
//     console.log(usersData, 'usersData')
//     if (todosData.userId === usersData.id) {
//       // const result =
//       return { todosData, usersData }
//     }
//   }
