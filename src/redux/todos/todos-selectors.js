import { createSelector } from '@reduxjs/toolkit'
// reselect
// export const getUsers = (state) => state.users
// export const getTodos = (state) => state.todos
// export const getCondition = (state) => state.users
// console.log(getUsers())
