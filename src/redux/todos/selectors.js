export const getUsers = (state) => state.data.users
export const getTodos = (state) => state.data.todos
export const isLoading = (state) => state.data.loading
export const toggleCompleted = (state) => state.data.toggleCompleted
